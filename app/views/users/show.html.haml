= avatar @user
%h1= @user.username
= link_to user_followed_users_path(@user) do
  = "#{@user.followed_users_count} following"
= link_to user_followers_path(@user) do
  = @user.followers_count
  = "follower".pluralize @user.followers_count
= follow_button @user
= render @timeline
